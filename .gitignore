# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.vercel

# Environment variables — never commit these!
# Match every .env variant (.env.test, .env.production, …), not just .env and
# .env.local — `vercel env pull` and `vercel build` drop secrets into these.
.env
.env.*
!.env.example
. c a c h e /  
 
# Next.js build output
.next/
out/

# Temporary / Scratch files
scratch/
temp.txt
test-static-map.js